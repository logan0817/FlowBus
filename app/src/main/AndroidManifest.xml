<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.logan.flowbusapp.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.logan.flowbusapp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.logan.flowbusapp.GlobalCaseActivity" />
        <activity android:name="com.logan.flowbusapp.DecisionGuideActivity" />
        <activity android:name="com.logan.flowbusapp.StickyCaseActivity" />
        <activity android:name="com.logan.flowbusapp.ScopeCaseActivity" />
        <activity android:name="com.logan.flowbusapp.ChannelCaseActivity" />
        <activity android:name="com.logan.flowbusapp.NonUiCaseActivity" />
        <activity android:name="com.logan.flowbusapp.login.LoginActivity" />
    </application>

</manifest>
